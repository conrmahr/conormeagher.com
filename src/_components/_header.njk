
<div class="{%- if topColor -%}{{topColor}}{%- endif -%} {% if page.url !== '/' %}md:pb-18 lg:pb-32{% endif %}">
  <header class="relative max-w-7xl mx-auto h-24 p-5 md:flex md:justify-between md:items-center">
      <!-- logo -->
      <a href="/" title="back to homepage" class="logo">
        <img src="{{metadata.author.avatar}}" class="rounded-full w-16 h-16 mb-4" alt="my avatar" width="64" height="64" />

      </a>
      <nav class="hidden md:block py-1">
        {% set navPages = collections.all | eleventyNavigation %}
        <ul class="text-right">
          {%- for entry in navPages %}
           {% if (loop.index) <= 2 %}
            <li class="inline-block mr-5">
              <a
              href="{{ entry.url | url }}"
              class="font-bold transition-colors dark:hover:text-yellow-200 hover:text-black
              {% if entry.url == page.url %} dark:text-yellow-200 text-black {% else %}dark:text-white text-gray-700 {% endif %}"
              >{{ entry.title }}</a>
            </li>
          {%- endif %}
          {% if (entry.key === 'resume.') %}
          <li class="inline-block">
            <a
            href="{{ entry.url | url }}"
            title="get in touch"
            target="_blank"
            rel="noreferrer"
            class="text-gray-800 dark:text-white py-2 px-5 rounded-full font-bold border-2 border-gray-900/20 dark:border-yellow-100 transition-colors hover:border-gray-900 dark:hover:border-yellow-100"
            ><span>{{ entry.title }}</span>
            </a>
          </li>
          {%- endif %}
        {%- endfor %}
        </ul>

      </nav>
  </header>
</div>